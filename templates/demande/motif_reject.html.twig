{% extends "base.html.twig" %}

{% block page_content %}
    				<div class="container">
						<div class="row">
                            <div class="col mt-5 my-flex-container">
                                <div class="mail-rejet">
                                    {{form_start(form)}}
                                    {{form_rest(form)}}
                                    {{form_end(form)}}
                                </div>
                            </div>
                        </div>
                    </div>
{% endblock page_content %}