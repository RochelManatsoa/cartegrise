{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body_id %}{% endblock %}

{% block class %}body-ccm{% endblock %}

{% block page_content %}
<div class="col-md-12 col-lg-8 d-flex flex-row justify-content-center my-flex-container align-items-center">
	<div class="row d-flex comment-ca-marche-texte flex-row-reverse my-flex-container">
        <div class="col-12 commander-firstview">
            <div class="row col-12 ">
                <div class="container">
                    <h2 class="text-center"> Formulaire pour <br> {{ commande.demarche.nom|upper }}</h2>
                    <br>
                        <hr>
                        {{ form_start(form, {'attr': {'class': 'row'}}) }}
                            <div class="col-4 mx-auto">
                                {% include "demande/caracteristicVehicle.html.twig" %}
                            </div>
                            <div class="col-3 mx-auto form-row">
                                <div class="col-11">
                                    <h5 class="text-center">{{ form_label(form.ctvo.ancienTitulaire) }}</h3>
                                        <hr>
                                        {% for antiTitulaire in form.ctvo.ancienTitulaire %}
                                            <div class="form-group">
                                                {{ form_label(antiTitulaire)}}
                                                {{ form_errors(antiTitulaire)}}
                                                {{ form_widget(antiTitulaire, {'attr': {'class': ''}})}}
                                            </div>
                                        {% endfor %}
                                        <br>
                                    
                                        <h3 class="text-center">Acquerreur</h3>
                                        <hr>
                                        {% for acquerreur in form.ctvo.acquerreur %}
                                            {% if loop.index < 7 %}
                                                <div class="form-group">
                                                    {{ form_label(acquerreur)}}
                                                    {{ form_errors(acquerreur)}}
                                                    {{ form_widget(acquerreur, {'attr': {'class': ''}})}}
                                                </div>
                                            {% endif %}
                                        {% endfor %}
                                </div>
                            </div>
                            <div class="col-3 mx-auto">
                                <div class="col-11">
                                    <h5> {{ form_label(form.ctvo.acquerreur.adresseNewTitulaire) }} </h5>
                                    {% for addresseNewTitulaire in form.ctvo.acquerreur.adresseNewTitulaire %}
                                        <div class="form-group">
                                            {{ form_label(addresseNewTitulaire)}}
                                            {{ form_errors(addresseNewTitulaire)}}
                                            {{ form_widget(addresseNewTitulaire, {'attr': {'class': ''}})}}
                                        </div>
                                    {% endfor %}
                                    <h5>{{ form_label(form.ctvo.cotitulaire) }}</h5>
                                    <ul class="tags" data-prototype="{{ form_widget(form.ctvo.cotitulaire.vars.prototype)|e('html_attr') }}">
                                        {% for tag in form.ctvo.cotitulaire %}
                                            {% if loop.index < 5 %}
                                            <li>
                                                {{ form_label(tag.vars.prototype)|e  }}
                                                {{ form_errors(tag.vars.prototype)|e  }}
                                                {{ form_widget(tag.vars.prototype, {'attr': {'class': ''}})|e  }}
                                            </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                </div>
                            </div>
                            {{form_rest(form)}}
                            <div class="col-12 text-center mb-5">
                                <br>
                                <input type="submit" class="btn btn-lg btn-success" value="Valider">
                            </div>
                        {{ form_end(form) }}
                </div>
            </div>
        </div>
	</div>
</div>
{% endblock %}
{% block javascripts %} 
{{ parent() }}
<script type="application/javascript">
    var $collectionHolder;

    // setup an "add a tag" link
    var $addTagButton = $('<button type="button" class="add_tag_link btn btn-primary btn-lg">Ajouter cotitulaire</button>');
    var $newLinkLi = $('<p></p>').append($addTagButton);

    jQuery(document).ready(function() {
        // Get the ul that holds the collection of tags
        $collectionHolder = $('ul.tags');
        // add a delete link to all of the existing tag form li elements
        $collectionHolder.find('li').each(function() {
            addTagFormDeleteLink($(this));
        });

        // add the "add a tag" anchor and li to the tags ul
        $collectionHolder.append($newLinkLi);

        // count the current form inputs we have (e.g. 2), use that as the new
        // index when inserting a new item (e.g. 2)
        $collectionHolder.data('index', $collectionHolder.find(':input').length);

        $addTagButton.on('click', function(e) {
            // add a new tag form (see next code block)
            addTagForm($collectionHolder, $newLinkLi);
        });
    });

    function addTagForm($collectionHolder, $newLinkLi) {
        // Get the data-prototype explained earlier
        var prototype = $collectionHolder.data('prototype');
    
        // get the new index
        var index = $collectionHolder.data('index');
    
        var newForm = prototype;
        // You need this only if you didn't set 'label' => false in your tags field in TaskType
        // Replace '__name__label__' in the prototype's HTML to
        // instead be a number based on how many items we have
        // newForm = newForm.replace(/__name__label__/g, index);
    
        // Replace '__name__' in the prototype's HTML to
        // instead be a number based on how many items we have
        newForm = newForm.replace(/__name__/g, index);
    
        // increase the index with one for the next item
        $collectionHolder.data('index', index + 1);
    
        // Display the form in the page in an li, before the "Add a tag" link li
        var $newFormLi = $('<p></p>').append(newForm);
        addTagFormDeleteLink($newFormLi);
        $newLinkLi.before($newFormLi);
    }

    function addTagFormDeleteLink($tagFormLi) {
        var $removeFormButton = $('<button type="button" class="btn btn-danger btn-lg">Supprimer ce cotitulaire</button>');
        $tagFormLi.append($removeFormButton);
    
        $removeFormButton.on('click', function(e) {
            // remove the li for the tag form
            $tagFormLi.remove();
        });
    }
</script>
    
{% endblock %}