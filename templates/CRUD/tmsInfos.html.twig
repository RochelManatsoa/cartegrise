<div class="col-md-12">
    <div class="box box-solid">
        <div class="box-header with-border">
            <h3 class="box-title">Informations de la demandes :</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <div class="box-group" id="accordion">
                {% for tmsInfo in tmsInfos %}
                    {% for key, tmsInf in tmsInfo %}
                        <div class="panel box box-success">
                            <div class="box-header with-border">
                                <h4 class="box-title">
                                    <a data-toggle="collapse" data-parent="#accordion" href={{'#collapseOne' ~ key}} aria-expanded="false" class="collapsed">
                                        {{ key }}
                                    </a>
                                </h4>
                            </div>
                            <div id={{'collapseOne' ~ key}} class="panel-collapse collapse in" aria-expanded="false" {% if loop.index != 1 %} style="height: 0px;overflow: auto;" {% endif %}>
                                <div class="box-body">
                                    {% if tmsInf is iterable %}
                                        {% for key2,tms in tmsInf %}
                                            {{key2}} => 
                                            {% if tms is iterable %}
                                                {% for key3,tm in tms %}
                                                    <br>{{key3}} => 
                                                    {% if tm is iterable %}
                                                        {% for key4, t in tm %}
                                                            <br>{{key4}} => 
                                                            {% if t is iterable %}
                                                                {% for key5, tn in t %}
                                                                    {{key5}} => {{ tn }}
                                                                {% endfor %}
                                                            {% else %}
                                                                {{t}}
                                                            {% endif %}
                                                    {% endfor %}
                                                    {% else %}
                                                        {{tm}} <br>
                                                    {% endif %}
                                                {% endfor %}
                                                <hr>
                                            {% else %}
                                                {{tms}}
                                            {% endif %}
                                            <br>
                                        {% endfor %}
                                    {% else %}
                                        {{tmsInf}}
                                    {% endif %}
                                    
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->
</div>