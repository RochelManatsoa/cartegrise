{% extends 'email_base.html.twig' %}


{% block body %}
    {% set transaction = findByTransactionId(responses.transaction_id) %}
        {% if responses.response_code == '00' %}
            {{ include('email/success.mail.twig') }}
        {% else %}
            {{ include('email/cancel.mail.twig') }}
        {% endif %}
{% endblock %}